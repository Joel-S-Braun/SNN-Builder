mouse = {}

mouse.position = vector2.new(0,0)
function get_selected()
	mouse_position.x = love.mouse.getX()
	mouse_position.y = love.mouse.getY()
	for name,child in pairs(workspace) do
		local ofst_x,ofst_y=
	end
end